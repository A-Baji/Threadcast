# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code — launch configs are developer-local, but settings.json is committed
# intentionally (format-on-save rules shared across the team).
# .vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio build artifacts
/android/app/debug
/android/app/profile
/android/app/release
android/build/reports/

# TTS model files — large binaries downloaded by setup-threadcast-env.ps1.
# Never commit these; they are installed locally by the setup script.
assets/tts_models/
kokoro-en-v0_19/

# Script backup files — created by migrate-dependencies.ps1 and
# setup-threadcast-env.ps1 as safety copies before overwriting Dart files.
# They are transient developer safety nets, not source-controlled artifacts.
*.bak
