import 'package:flutter/material.dart';

class UrlChipList extends StatelessWidget {
  const UrlChipList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('UrlChipList — TODO')),
    );
  }
}