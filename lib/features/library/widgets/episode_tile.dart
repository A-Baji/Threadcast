import 'package:flutter/material.dart';

class EpisodeTile extends StatelessWidget {
  const EpisodeTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('EpisodeTile — TODO')),
    );
  }
}
